# Frontend environment variables (copy to .env and edit as needed)

# Vite env vars must be prefixed with VITE_
# Base URL for API; default dev proxy points to http://localhost:8000 in vite.config.ts
# If deploying without proxy, set for absolute URLs, e.g. https://api.textpress.md
VITE_API_BASE=/api


